alt + shift + o
 dwmc zoom

alt + q ; w ; {1,2,3,4,5,6,7,8,9}
 dwmc setlayoutex {1,2,3,4,5,6,7,8,9}

alt + q ; f
 warpd --hint

alt + q ; u
 warpd --hint

alt + q ; v
 warpd --normal

alt + q ; colon
 spmenu

alt + Tab 
 rofi -show window

alt + F1 
 rofi -show drun

alt + q ; o
 alacritty 

alt + q ; q 
 dwmc killclient
      
alt + q ; O 
 dwmc zoom

alt + q ; B
 dwmc togglebar

alt + q ; {1-9}
 dwmc view {0-9}

alt + q ; Return
 alacritty

alt + q ; O
 alacritty -e emacs -nw  

alt + {j,k}
 dwmc focusstack {+,-}1

alt + q ; o
 dwmc focusstack +1

alt + q ; 0
 dwmc killclient

alt + bracketleft
 dwmc killclient

alt + q ; n ; m
 alacritty -e "sudo bandwhich"

alt + q ; K
 kleopatra

alt + v
 exec /home/hexe/Applications/Chatbox-1.3.12-x86_64_310e5363068bf30d5f61cba692ff230f.AppImage --no-sandbox %U
    	
alt + q ; b
 qutebrowser 

alt + q ; W
 nitrogen

shift + space ; {1-9}
 dwmc tagex {0-9}

alt + q ; B
 alacritty -e "cd ~/suckless/widow2/ && make && sudo make clean install" 

alt + q ; e
 alacritty -e emacs -nw 
   
Print 
 spectacle -r

shift + Print
 spectacle -f
      
alt + q ; t ; m 
 alacritty -e mitmproxy

alt + W ; w
 wireshark

alt + q ; E
 exec /home/hexe/Applications/eDEX-UI-Linux-x86_64_eecd3a9dec6a5328bb312833d8ae1604.AppImage 

alt + W ; W
 alacritty -e spiderfoot -l 127.0.0.1:7777"

alt + W ; m 
 alacritty -e metasploit-framework.msfconsole

alt + q ; y
 freetube

alt + q ; m
 alacritty -e ncmpcpp 

alt + q ; M
 nicotine

alt + q ; E
 jamesdsp

alt + q ; c ; R
 mpc repeat on; 
 notify-send "REPEAT: ALL"

alt + q ; c ; r
 mpc repeat off; 
 notify-send "REPEAT: NONE"

alt + q ; c ; S
 mpc single on; 
 notify-send "REPEAT: SINGLE"

alt + q ; c ; s
 mpc single off; 
 notify-send "REPEAT: OFF"

alt + q ; c ; p
 mpc toggle; 
 if mpc status | grep -q "playing"; then
     notify-send "PLAYING: TRUE"
 else
     notify-send "PLAYING: FALSE"
 fi

alt + XF86AudioRaiseVolume
 playerctl -a volume 0.1+ 

alt + XF86AudioLowerVolume
 playerctl -a volume 0.1- 

alt + q ; c ; u
 alacritty -e mpc update && sleep 3;
 notify-send "MUSIC DB UPDATING"

alt + q ; c ; c 
 notify-send "$(mpc current)" 

alt + q ; M
 env LD_PRELOAD=/usr/lib/spotify-adblock.so spotify --uri=%U
    
alt + q ; r
 goodvibes

alt + q ; R
 proxychains flatpak run de.haeckerfelix.Shortwave

alt + q ; I  
 armcord

alt + q ; F 
 alacritty -e fff

alt + q ; C 
 rog-control-center

alt + W ; d ; i
 gnome-dictionary

alt + q ; ctrl + i
 riseup-vpn

# redshift (will go to system mode)
alt + q ; s
 redshift -O 3000

alt + q ; S
 redshift -x

alt + q ; x 
 xkill

#alt + W ; n 
# alacritty -e nikto -h $(xsel -b) -C all
# add to pentest mode. 

alt + q ; h   
 dunstctl close-all

alt + q ; H 
 dunstctl history-pop

ctrl + S 
 redshift -e 

alt + q ; k
 keepassxc

alt + W ; f
 freeplane      

alt + n   
 sh ~/labyrinth -v  

alt + q ; n
 sh ~/labyrinth -n

alt + q ; d 
 sh ~/labyrinth -d 

alt + q ; a      
 sh ~/labyrinth -a  

alt + Escape
 sh ~/labyrinth -i

alt + grave
 sh ~/labyrinth -v

alt + q ; i 
 sh ~/labyrinth -i

ctrl + bracketleft
 sh ~/labyrinth -v


alt + q ; T
 blueman-manager

alt + q ; ctrl + d
 bitwig-studio
